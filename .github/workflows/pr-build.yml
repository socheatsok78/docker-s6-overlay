name: PR Build

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review, labeled]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:

  prepare:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.generate.outputs.matrix }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Generate matrix
        id: generate
        run: ./.github/scripts/generate-pr-build-matrix.sh

  s6-overlay:
    needs:
      - prepare
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.prepare.outputs.matrix) }}
    permissions:
      contents: read
      packages: write
    uses: socheatsok78/uses/.github/workflows/docker-buildx-bake.yml@main
    with:
      flavor: latest=false
      source: "{{defaultContext}}:library/${{ matrix.version }}"
      tags: type=raw,value=${{ matrix.version }}
      enable-dockerhub: true
      permit-login: ${{ contains(github.event.pull_request.labels.*.name, 'pr-pull') }}
      permit-push: ${{ contains(github.event.pull_request.labels.*.name, 'pr-pull') }}
      permit-cache: ${{ contains(github.event.pull_request.labels.*.name, 'pr-pull') }}
    secrets: inherit

  pr-merge:
    if: contains(github.event.pull_request.labels.*.name, 'pr-pull')
    runs-on: ubuntu-latest
    needs:
      - s6-overlay
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v7
      - name: Evaluate the PR for merging
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run:
          gh pr merge --repo ${{ github.repository }} --merge ${{ github.event.pull_request.number }} --delete-branch
